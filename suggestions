总结
必须修改：
impl/Compiler.java（主逻辑）。
framework/lang/Type.java及其子类（类型系统）。
新增 SymbolTable.java（符号表管理）。
不能修改：
所有 generated/下的文件。
AbstractCompiler, AbstractGrader, Grader, Main, Utils。
Splc.g4：不需要修改（即使它存在）。
